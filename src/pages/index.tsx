import * as React from "react";

const FrontPage = ({}) => {
    const message: string = "ello World";
    return (
        <div>
            { message }
        </div>
    );
}

export default FrontPage;